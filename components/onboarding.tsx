'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { Activity, ArrowLeft, ArrowRight, HeartPulse, MapPin, ShieldCheck, ThermometerSun, Wind } from 'lucide-react'
import { IndiaMap, cities } from './india-map'

type Choice = 'yes' | 'no' | null

function ChoiceField({ label, icon: Icon, value, onChange }: { label: string; icon: typeof HeartPulse; value: Choice; onChange: (value: Choice) => void }) {
  return <fieldset className="space-y-2.5"><legend className="flex items-center gap-2 text-sm font-semibold text-slate-800"><Icon className="h-4 w-4 text-teal-600" />{label}</legend><div className="grid grid-cols-2 gap-2">{(['yes', 'no'] as const).map((option) => <button key={option} type="button" aria-pressed={value === option} onClick={() => onChange(option)} className={`rounded-xl border px-4 py-3 text-sm font-semibold capitalize transition ${value === option ? 'border-teal-600 bg-teal-50 text-teal-700 shadow-sm' : 'border-slate-200 bg-white text-slate-500 hover:border-teal-300 hover:text-slate-700'}`}>{option}</button>)}</div></fieldset>
}

export function Onboarding({ initialStep = 1 }: { initialStep?: 1 | 2 }) {
  const router = useRouter()
  const [step, setStep] = useState(initialStep)
  const [age, setAge] = useState('')
  const [cardiovascular, setCardiovascular] = useState<Choice>(null)
  const [respiratory, setRespiratory] = useState<Choice>(null)
  const [manualLabor, setManualLabor] = useState<Choice>(null)
  const [selectedCity, setSelectedCity] = useState(cities[3])
  const canContinue = age !== '' && cardiovascular !== null && respiratory !== null && manualLabor !== null

  return <main className="min-h-screen bg-[#f5f8f7] text-slate-900"><header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 sm:px-8 lg:px-10"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-teal-700 text-white"><ThermometerSun className="h-5 w-5" /></div><div><p className="text-base font-bold tracking-tight">HeatShield</p><p className="text-[10px] font-semibold uppercase tracking-[0.2em] text-slate-400">India heat intelligence</p></div></div><div className="hidden items-center gap-2 text-xs font-medium text-slate-500 sm:flex"><ShieldCheck className="h-4 w-4 text-teal-600" /> Your data stays private</div></header><section className="mx-auto max-w-3xl px-5 pb-12 pt-4 sm:px-8"><div className="mb-8"><p className="mb-3 flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] text-teal-700"><Activity className="h-4 w-4" /> Step {step} of 2</p><h1 className="text-4xl font-bold tracking-tight text-slate-950 sm:text-5xl">{step === 1 ? <>Know your heat risk <span className="text-teal-700">before</span> you step out.</> : <>Choose your location for a sharper forecast.</>}</h1><p className="mt-4 max-w-xl text-base leading-7 text-slate-500">{step === 1 ? 'A few details help us make heat-wave alerts more relevant to your body and your work.' : 'Tap a city marker or city button to explore the current heat risk.'}</p></div>{step === 1 ? <section className="rounded-[1.5rem] border border-slate-200 bg-white p-6 shadow-sm sm:p-7"><div className="mb-7"><h2 className="text-lg font-bold">About you</h2><p className="mt-1 text-sm text-slate-500">Used only to personalize your score.</p></div><div className="space-y-6"><label className="block space-y-2.5"><span className="text-sm font-semibold text-slate-800">What is your age?</span><div className="relative"><input aria-label="Age in years" type="number" min="1" max="120" step="1" inputMode="numeric" value={age} onChange={(e) => setAge(e.target.value)} placeholder="Enter age" className="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 pr-20 text-sm outline-none transition placeholder:text-slate-400 focus:border-teal-500 focus:bg-white focus:ring-4 focus:ring-teal-500/10" /><span aria-hidden="true" className="pointer-events-none absolute right-4 top-1/2 -translate-y-1/2 text-sm text-slate-400">years</span></div></label><ChoiceField label="Cardiovascular disease" icon={HeartPulse} value={cardiovascular} onChange={setCardiovascular} /><ChoiceField label="Respiratory issues" icon={Wind} value={respiratory} onChange={setRespiratory} /><ChoiceField label="Outdoor manual labor" icon={Activity} value={manualLabor} onChange={setManualLabor} /></div><button type="button" disabled={!canContinue} onClick={() => router.push('/map')} className="mt-8 flex w-full items-center justify-center gap-2 rounded-xl bg-teal-700 px-5 py-3.5 text-sm font-bold text-white shadow-lg shadow-teal-700/20 transition hover:bg-teal-800 disabled:cursor-not-allowed disabled:opacity-40">Continue to location <ArrowRight className="h-4 w-4" /></button></section> : <><section><div className="mb-4 flex items-end justify-between"><div><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] text-teal-700"><MapPin className="h-4 w-4" /> Your location</p><h2 className="mt-1 text-2xl font-bold tracking-tight">Where are you today?</h2></div><p className="hidden text-right text-xs leading-5 text-slate-400 sm:block">Tap a marker<br />to explore</p></div><IndiaMap onCityChange={setSelectedCity} /><div className="mt-4 grid grid-cols-3 gap-3"><div className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">HTSI</p><p className="mt-1 text-xl font-bold text-orange-600">{selectedCity.score}<span className="text-sm">/100</span></p><p className="mt-1 text-[10px] text-slate-400">Heat Thermal Stress Index</p><p className="text-[11px] text-slate-500">Wet-bulb globe</p></div><div className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Risk score</p><p className="mt-1 text-xl font-bold text-orange-600">{selectedCity.score}<span className="text-sm">/100</span></p><p className="text-[11px] text-slate-500">Personalized</p></div><div className="rounded-xl border border-orange-200 bg-orange-50 p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-orange-600">Alert level</p><p className="mt-1 text-xl font-bold text-orange-700">{selectedCity.risk}</p><p className="text-[11px] text-orange-700/70">Take precautions</p></div></div></section><button type="button" onClick={() => setStep(1)} className="mt-6 flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-5 py-3.5 text-sm font-bold text-slate-700 hover:border-teal-400"><ArrowLeft className="h-4 w-4" /> Back to personal details</button></>}</section></main>
}
